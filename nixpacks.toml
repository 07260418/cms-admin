[phases]
  [phases.setup]
  nixPkgs = ["gcc", "gnumake", "python3", "pkg-config"]

  [phases.install]
  dep_commands = ["npm install --build-from-source"]

  [phases.build]
  cmds = ["echo 'build complete'"]

[start]
  cmd = "node server.js"
